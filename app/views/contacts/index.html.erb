<h1>Your Contacts</h1>

<% current_user.contacts.each do |contact| %>
  <%= link_to contact.name, contact_path(contact)%> | <%= contact.phone %>
  <br>
<% end %>

<%= link_to 'Add Contact', new_contact_path %>
<%= link_to 'Send to multiple people', new_message_path %>
